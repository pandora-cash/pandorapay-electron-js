{
  "name": "pandorapay-electron-js",
  "main": "index.js",
  "description": "wallet",
  "author": "pandora cash",
  "version": "0.0.1",
  "scripts": {
    "start": "electron .",
    "build": "electron-packager . --overwrite --out=release-builds",
    "builder": "electron-builder -mwl --config electron-builder.js"
  },
  "devDependencies": {
    "electron": "^20.1.2",
    "electron-builder": "^23.3.3",
    "electron-packager": "^15.5.1"
  },
  "dependencies": {
    "dotenv": "^16.0.2"
  }
}
